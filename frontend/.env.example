# URL da API (backend)
VITE_API_URL=http://localhost:8000

# Chave de acesso ao backend (mesmo valor de BACKEND_ACCESS_KEY no backend/.env)
# Obrigatória quando BACKEND_ACCESS_ENABLED=true
VITE_BACKEND_ACCESS_KEY=cole-a-mesma-chave-do-backend
